import { rework } from 'dummy/helpers/rework';
import { module, test } from 'qunit';

module('Unit | Helper | rework');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rework([42]);
  assert.ok(result);
});
